requests~=2.34.2
Pillow~=12.2.0
PyYAML~=6.0.3
soco~=0.31.1
Flask~=3.1.3
adb-shell~=0.4.4
pychromecast~=13.1.0
spotipy~=2.26.0
paho-mqtt~=2.1.0
flask-sock~=0.7.0
pyatv~=0.17.0
ruamel.yaml~=0.19.1
pydantic~=2.13.4
# MPD source (mediainfo/sources/mpd.py) - a small, mature client for MPD's
# own text protocol, so this codebase doesn't have to own protocol parsing.
python-mpd2~=3.1.1
# Optional Pixoo text-detection stage (mediainfo/text_removal.py) - only
# exercised when outputs.pixoo.text_detection_enabled is set, but always
# installed (matches this project's flat-requirements convention - no
# optional-extras groups) so the feature works out of the box once a user
# supplies a model file, no separate install step needed.
opencv-python-headless~=5.0.0
numpy~=2.2.6
# Home Assistant source (mediainfo/sources/homeassistant.py) - a small,
# synchronous client for HA's WebSocket API, run in its own background
# thread (see WebSocketApp.run_forever()) rather than a second asyncio loop.
websocket-client~=1.9.0
